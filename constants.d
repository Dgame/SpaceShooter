module Spaceshooter.constants;

public {
	enum float Slide = 0.8f;
	enum short Move = 15;

	enum ushort WinWidth = 640;
	enum ushort WinHeight = 480;
	
	enum ubyte ShotWait = 80;
	enum ubyte MaxEscape = 2;
}